apply2 = (f: (a: i32, b: i32) i32, v: i32) i32 { f(v, v) }

// Wrapping a one-argument closure cannot hide its arity.
main = (env: Env) Res<i32, AllocError> {
    r = apply2((a: i32) i32 { a }, 6);
    println("r={}", r);
    Ok(0);
}
