pick = <T: ToU64>(value: T, body: (value: T) T) i32 { 1 }
pick = (value: i32, body: (value: i32) i32) i32 { 2 }

main = (env: Env) Res<i32, AllocError> {
    value: i32 = 1;
    pick(value, (item: u32) { item });
    Ok(0)
}
