// a backslash followed by q is not an escape sequence, in a char
// literal either.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    c: u8 = '\q';
    println("{}", c);
    Ok(0);
}
