Link = {
    value: i64,
    next: Ptr<Link>,
}

build = (n: i64, rest: Ptr<Link>) Link {
    (n <= 0).match({
        true  => rest.to(),
        false => build(n - 1, null_ptr<Link>()),
    });
}

main = (env: Env) Res<i32, AllocError> {
    println("{}", make_chain(3).value);
    Ok(0);
}
