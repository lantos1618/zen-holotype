Bundle = schema.schema

carry = <T>(value: T) T { value }

make = <T>(value: T) Bundle<T> {
    bundle = Bundle<T>();
    println("{}", carry<T>(value));
    bundle
}

main = () () {
    narrow = make<u8>(3);
    wide   = make<i64>(4);
    println("{} {}", narrow.slot.marker, narrow.slot.data.is_null());
    println("{} {}", wide.slot.marker, wide.slot.data.is_null());
}
