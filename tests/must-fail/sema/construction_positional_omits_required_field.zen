// A positional supply does not disable the missing-field check.
// position: the incomplete construction.

Point = {
    x: i32,
    y: i32,
}

main = (env: Env) Res<i32, AllocError> {
    p = Point(7);
    println("{}", p.y);
    Ok(0);
}
