// Public actor surface. Context lives separately from the core address and
// protocol types so Env can declare spawn without importing Env back.

Actor*, Ref*, ActorError*, ActorStartError* = std.actor.actor_core
Context* = std.actor.actor_context
