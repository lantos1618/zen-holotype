// probeO: no Drop at all -- callee prints via println sugar; println-arg call
three = (env: Env) i64 {
    f = NoDrop(env: env, tag: "f");
    env.out.println("inside three");
    3
}

NoDrop = {
    env: Env,
    tag: str,
}

main = (env: Env) Res<i32, IoError> {
    println("chain {}", three(env));
    Ok(0);
}
