{ to_exit } = std.core.bool
Box<T>: {
    value: T,
    get = (box: Box<T>) T { box.value }
}
Msg*: Hold(Box<StringView>) | Empty
main = () i32 {
    msg := Msg.Hold(Box<StringView>(value: "generic"))
    got := msg.match ({
        .Hold(box) => box.get(),
        .Empty     => "empty",
    })
    (got == "generic").to_exit()
}
