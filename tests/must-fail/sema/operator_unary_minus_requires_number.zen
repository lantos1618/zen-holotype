// Unary minus has the same numeric domain as ordinary arithmetic. It cannot
// hand a string through to C's unary operator.

negate = (text: str) str { -text }

main = (env: Env) Res<i32, AllocError> { Ok(0) }
