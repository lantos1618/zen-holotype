{ Rect } = core.rect

width*  = (r: Ptr<Rect>) i32 { r.w }   // read-only accessors take a plain Ptr
height* = (r: Ptr<Rect>) i32 { r.h }

// grows the rectangle in place — it WRITES r.w, so it needs a mutable pointer.
grow* = (r: MutPtr<Rect>) i32 { r.w = r.w + 1  r.w }
