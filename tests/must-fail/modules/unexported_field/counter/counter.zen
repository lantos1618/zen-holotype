// total is readable outside the module; step is not

Counter* = {
    total* :: i32,
    step :: i32 = 2,

    add* = (self :: @Self, n: i32) () {
        self.total = self.total + n * self.step;
    }
}
