main = (env: Env) Res<i32, IoError> {
    byte: u8  = 0xff;
    word: u16 = 0xBEEF;
    println("{} {} {} {}", 0x0, 0x2a, 0XFF, 0xffffffffffffffff == u64.MAX);
    println("{} {}", byte, word);
    Ok(0)
}
