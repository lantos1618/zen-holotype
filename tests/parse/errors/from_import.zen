// MUST FAIL: `from ... import ...`. DESIGN.md:287 — "No `export`, no `from`".
// An import is an ordinary binding whose right-hand side is a module path:
// `Res, Ok, None = std.core.result` (DESIGN.md:291). `import` is not a keyword
// either; DESIGN.md:996 calls imports "just bindings".

from std.core.result import Res, Ok, None
