{
  "name": "tree-sitter-zen",
  "version": "0.1.0",
  "description": "tree-sitter grammar for the Zen subset compiled by zen",
  "private": true,
  "//": "Source-only: the compiler parses via Python (tree_sitter build_library over src/parser.c). No Node bindings are built or published here, so the node-gyp/prebuildify scaffold has been removed. `tree-sitter generate` reads grammar.js.",
  "tree-sitter": [
    {
      "scope": "source.zen",
      "injection-regex": "^zen$"
    }
  ]
}
