// the bound: an ordinary struct. nothing marks it special, because
// nothing needs to -- a bound is "which struct's shape do I need".

Boxy* = {
    width: i32,
    height: i32,

    area* ::= (self: @Self) i32 { self.width * self.height }
}
