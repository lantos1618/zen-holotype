make = (a: Alloc) Res<i32, AllocError> {
    p = a.create<i32>().try();
    p.write(0, 42);
    Ok(p.read(0))
}

main = (env: Env) Res<(), AllocError> {
    println("{}", make(env.mem.alloc()).try());
    Ok(())
}
