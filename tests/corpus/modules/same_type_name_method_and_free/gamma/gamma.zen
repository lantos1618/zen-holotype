// gamma sees BETA's Diag and calls the method on it. main never imports
// beta, so nothing in one file names both types: the collision is between
// two modules of one PROGRAM, which is where it actually happened
Diag = beta.beta

shout* = (n: usize) usize {
    d = Diag(note: n);
    d.render();
}
