// the block comment below never closes, so it swallows the whole
// program including main. the diagnostic must name the comment, not
// the missing main.
Error = AllocError | IoError

/* opening a comment here
main = (env: Env) Res<i32, Error> {
    println("hi");
    Ok(0);
}
