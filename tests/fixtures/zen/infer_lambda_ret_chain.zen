{ Result, IoError, expect, map_err } = std.core.result
{ to_exit } = std.core.bool
main = () i32 {
    good: Result<i32, IoError> := .Ok(42)
    v := good.map_err((e) { "boom" }).expect("mapped")
    (v == 42).to_exit()
}
