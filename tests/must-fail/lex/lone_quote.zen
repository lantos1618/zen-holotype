// a lone ' with nothing after it: it opens a char literal that swallows
// the rest of the line and never closes.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    c: u8 = ';
    println("{}", c);
    Ok(0);
}
