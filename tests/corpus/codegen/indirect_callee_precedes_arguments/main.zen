tick = () i32 { println("arg"); 5 }

use = (pick: (value: i32) i32, value: i32) i32 {
    (pick)(value)
}

main = (env: Env) Res<i32, AllocError> {
    println("{}", use((value: i32) i32 { println("callee"); value }, tick()));
    Ok(0)
}
