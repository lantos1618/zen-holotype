halloc = std.mem.heap
vec = std.collections.vec
{ println } = std.text.fmt
main = () i32 {
  alloc := halloc.gpa()
  a := alloc.addr()
  v := vec.of(a, [10, 20])
  v2 := v.push(a, 30)
  println(v2.get(0))
  println(v2.get(2))
  0
}
