// A positional argument after the final storage seat is never discarded.
// position: the first surplus argument.

Single = {
    value: i32,
}

main = (env: Env) Res<i32, AllocError> {
    s = Single(41, "lost");
    println("{}", s.value);
    Ok(0);
}
