Red*: {
    _: i32,
    turnBlue = (r: Red) Blue { Blue(_: 0) }
    score = (r: Red) i32 { 10 }
}
Blue*: {
    _: i32,
    score = (b: Blue) i32 { 32 }
}
main = () i32 {
    r := Red(_: 0)
    b := r.turnBlue()
    r.score() + b.score()
}
