Error = AllocError | IoError

// a declaration whose body is nothing but comments, and comments in
// every position a token could go
main /* name */ = /* eq */ (env: Env) /* params */ Res<i32, Error> /* ret */ {
    // only comments here
    /* and here */
    println("body reached");
    // and a comment as the last thing before the closing brace
    Ok(0);
}
// a comment after the last declaration
