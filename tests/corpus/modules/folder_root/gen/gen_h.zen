// siblings are ordinary modules, not nested ones: gen_h reaches gen_c
// by path. unstarred, so backend_name stays local to this file

backend_name = gen.gen_c

emit_h* = (n: i32) i32 { n + 100 }

header_name* = () str { backend_name() }
