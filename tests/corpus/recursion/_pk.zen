// probeK: callee with TWO printlns as println arg
three = (env: Env) i64 {
    env.out.println("one");
    env.out.println("two");
    3
}

main = (env: Env) Res<i32, IoError> {
    println("chain {}", three(env));
    Ok(0);
}
