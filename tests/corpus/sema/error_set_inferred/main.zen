CfgError, read_cfg = cfg.cfg

main = (env: Env) Res<i32, CfgError> {
    println("{}", read_cfg("7").try());
    read_cfg("x").match({
        Ok(_)  => println("ok"),
        Err(_) => println("bad"),
    });
    Ok(0);
}
