// the root publishes the sibling's type and adds one of its own: a folder
// controls its surface, so re-exporting and declaring sit in the same file
Circle*, area* = geo.geo_circle

unit* = () Circle { Circle(radius: 1) }
