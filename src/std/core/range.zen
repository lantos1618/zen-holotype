Res = std.core.result

// A half-open interval `[start, end)`. Implementations may map each index to a
// value with `at`; None ends iteration early.
Range*<T> = {
    start*: usize,
    end*: usize,

    at* ::= (self: @Self, index: usize) Res<T>
}
