// probeJ: same, but the callee prints via println sugar instead of Drop
three = (env: Env) i64 {
    env.out.println("inside");
    3
}

main = (env: Env) Res<i32, IoError> {
    println("chain {}", three(env));
    Ok(0);
}
