Cell<T> = { value :: T }
store = <T>(cell :: Cell<T>, value: T) () { cell.value = value; }
bad = (env: Env, cell :: Cell<str>) Res<(), AllocError> {
    a    = env.mem.alloc();
    text = a.String("hello").try();
    store(cell, text.view());
    Ok(())
}
main = (env: Env) Res<(), AllocError> {
    cell ::= Cell<str>(value: "");
    bad(env, cell).try();
    Ok(())
}
