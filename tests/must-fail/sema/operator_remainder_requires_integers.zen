// Division accepts floats, but remainder is an integer-only operation: C has
// no `%` operator for f64 values.

remainder = (left: f64, right: f64) f64 { left % right }

main = (env: Env) Res<i32, AllocError> { Ok(0) }
