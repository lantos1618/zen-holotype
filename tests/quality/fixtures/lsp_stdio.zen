// Standalone transport entry for sanitizer-backed LSP process regressions.
serve_stdio = lsp.lsp_stdio

main = (env: Env) Res<i32, AllocError> {
    arena ::= env.mem.alloc();
    serve_stdio(env, arena)
}
