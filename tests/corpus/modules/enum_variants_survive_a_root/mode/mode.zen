// a variant is exported with its enum, and the root may publish it under
// its own name as well, so a consumer can write `One` unqualified
Tag*, One*, Two*, Three* = mode.mode_tag
Only*, Single*           = mode.mode_tag
