// TWO fields, with two distinct non-default values at the one place they
// are constructed: a struct that arrives by two paths has to arrive the
// same way down both, and one field cannot say whether it did.
Token* = {
    id*: i32,
    tag*: i32,
}

token_id* = (t: Token) i32 { t.id }
