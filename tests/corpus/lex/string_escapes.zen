// the escapes a string literal must accept, and the exact bytes each
// one produces.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("tab:\there");
    println("newline:\nsecond");
    println("backslash:\\");
    println("quote:\"");
    println("all four:\t\\\"|");
    Ok(0);
}
