Box = { text :: str }
put = (box :: Box, text: str) () { box.text = text; }
main = (env: Env) Res<(), AllocError> {
    box ::= Box(text: "");
    bad(env, box).try();
    println(box.text);
    Ok(())
}
bad = (env: Env, box :: Box) Res<(), AllocError> {
    a    = env.mem.alloc();
    text = a.String("hello").try();
    put(box, text.view());
    Ok(())
}
