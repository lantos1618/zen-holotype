// A written argument settles T before values are checked against the resulting
// signature. `id<str>` therefore cannot accept an i32.

id = <T>(value: T) T { value }

main = (env: Env) Res<i32, AllocError> {
    number: i32 = 1;
    println("{}", id<str>(number));
    Ok(0);
}
