// MUST FAIL: `@self`. The compiler namespace holds `@Self` — "the type being
// declared" (DESIGN.md:302) — not `@self`. The receiver is an ordinary parameter
// named `self` with no `@` (DESIGN.md:223); `@self` is neither.

len* = (self: @self) usize {
    self.len
}
