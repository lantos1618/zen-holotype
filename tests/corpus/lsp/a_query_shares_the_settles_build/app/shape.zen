// The OTHER module. One line for the struct, so the expected file's
// columns are easy to read: `x` is the member at 0:11, `y` at 0:20.
Point* = { x*: i32, y*: i32 }

sum* = (p: Point) i32 {
    p.x + p.y
}
