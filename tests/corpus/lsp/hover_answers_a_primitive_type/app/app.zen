// The on-disk copy of the document the harness opens. The overlay
// carries the same bytes; the sibling test
// (`hover_answers_an_imported_name`) is the one that pins buffer vs
// disk, so this file is only here to give the build a path to name.
note = (b: bool, s: str, n: i32) bool {
    b
}
