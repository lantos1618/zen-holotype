// the other `Diag`. same top-level name, same METHOD name, different
// arity -- the pair that made the C compiler report
// "expected gen..Diag but argument is of type sema..Diag"
Diag* = {
    note*: usize,
    render* = (self: @Self) usize { self.note * 10 }
}
