// module `alpha_beta`.
//
// the other half of the collision. `value` is also defined by module
// `alpha`; that one must keep working, since DESIGN.md § Modules promises
// two modules may define the same top-level name.

gamma* = () i32 { 2 }

value* = () i32 { 20 }

Gamma* = {
    tag: i32
}
