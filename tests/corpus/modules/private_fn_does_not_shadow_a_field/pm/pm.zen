Fn* = {
    name*: usize,
    body*: usize,
}

// private: no `*`, so this name does not cross pm's boundary in any
// direction -- least of all to shadow the exported field beside it
body = (n: usize) usize { n + 1 }

make* = (n: usize) Fn { Fn(name: n, body: body(n)) }
