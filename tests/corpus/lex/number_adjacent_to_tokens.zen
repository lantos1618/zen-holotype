// numbers with no whitespace around them: 1+2 must lex as three tokens,
// and 1.5 must not become 1 . 5.
Error = AllocError | IoError

same = (a: f64, b: f64) str {
    (a == b).match({
        true  => "yes",
        false => "no",
    });
}

main = (env: Env) Res<i32, Error> {
    a:i32=1+2;
    b:i32=3*4;
    println("{}", a);
    println("{}", b);
    println("{}", same(1.5, 1.5));
    Ok(0);
}
