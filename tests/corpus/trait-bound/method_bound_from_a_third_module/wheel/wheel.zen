// the type, and its impl of a bound declared in another module.
// importing Wheel pulls its world along, impls included.

Meter = meter.meter

Wheel* = {
    psi: i32,
}

Wheel.impl(Meter, {
    read = (self: @Self) i32 { self.psi + 3 }
})
