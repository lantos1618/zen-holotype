// a utf-8 BOM in the MIDDLE of the file. only a LEADING BOM is
// stripped; anywhere else it is an ordinary, and invalid, byte
// sequence.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
﻿    println("hi");
    Ok(0);
}
