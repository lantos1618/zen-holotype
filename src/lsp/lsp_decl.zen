// src/lsp/lsp_decl.zen
// MOVED TO `src/std/ast/ast_named.zen`, verbatim, the day `definition`
// became the second caller (`STYLE.md`'s second-caller rule, and this
// file's own header said so while it lived here). What remains is the
// re-export, so `lsp_hover.zen` and the folder root keep their imports
// and the move costs nobody a call-site edit.

Tell*, TypeAt*, ValueAt*, told_at*, is_nothing* = std.ast.ast_named
