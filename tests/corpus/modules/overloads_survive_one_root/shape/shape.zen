// two siblings declare `label`. the root publishes both, so the overload set
// survives the hop instead of the first one winning
Circle*, label* = shape.shape_circle
Square*, label* = shape.shape_square
