// Ptr<()> is an opaque address with no element to dereference or step over.

main = (env: Env) Res<i32, AllocError> {
    p = null_ptr<()>();
    p.read(0);
    p.write(0, ());
    p.offset(1);
    p.back(1);
    Ok(0)
}
