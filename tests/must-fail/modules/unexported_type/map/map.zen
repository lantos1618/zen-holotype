// DESIGN's other named case: Entry is internal to the module

Entry = {
    hash: i32,
    key: i32,
}

Map* = {
    count* :: i32,

    set* = (self :: @Self, key: i32) () {
        e = Entry(hash: key, key: key);
        self.count = self.count + e.key;
    }
}
