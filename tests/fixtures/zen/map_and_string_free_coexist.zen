halloc = std.mem.heap
maps = std.collections.map
{ String, init } = std.text.string
main = () i32 {
    alloc := halloc.gpa()
    a := alloc.addr()
    w := maps.of(a, "x", 7)
    s := a.init(4).push_in(a, '!')
    n := w.get("x", 0) + to_i32(s.len)
    w.free(a)
    s.free_in(a)
    n
}
