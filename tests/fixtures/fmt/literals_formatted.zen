{ println } = std.text.fmt
main = () i32 {
    println("// { not a comment")
    println('}')
    (1 == 1).match ({ true => {
        // } comment should not close block
        println("{")    // } trailing comment keeps text
        0
    }, false => 1 })
}
