// A module-qualified free function keeps ordinary generic-call semantics; it
// is neither a constructor nor an associated member of the module alias.

mod = lib

main = (env: Env) Res<i32, AllocError> {
    value: str = "wrong";
    println("{}", mod.id<i32>(value));
    Ok(0);
}
