// MUST FAIL: a function type whose parameters are unnamed. DESIGN.md:329 —
// "Function types must name their parameters: `(i32, i32) i32` says nothing about
// which `i32` is which." Only `() ()` is exempt, having nothing to name.

apply* = (f: (i32, i32) i32, a: i32, b: i32) i32 {
    f(a, b)
}
