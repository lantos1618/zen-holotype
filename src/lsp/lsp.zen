// The language server: `serve` answers one framed session from a buffer,
// `serve_stdio` runs it over stdin/stdout. Everything else is reached
// through its own module.

Server*, serve*                     = lsp.lsp_serve
serve_stdio*                        = lsp.lsp_stdio
frame_at*, short_by*, write_frame*  = lsp.lsp_frame
