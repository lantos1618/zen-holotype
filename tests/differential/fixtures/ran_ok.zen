// An accepted program reaches native execution and preserves a zero result.
main = (env: Env) Res<i32, AllocError> {
    (env.argv.len == 0).match({
        true  => Ok(0),
        false => Ok(0),
    })
}
