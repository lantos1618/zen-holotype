// the module the walk below finds. A folder carries its root beside
// its children, so `probe/probe.zen` is module `probe` -- and that is
// a rule a path is COMPUTED from, never one a listing discovers.
depth* = () i32 { 1 }
