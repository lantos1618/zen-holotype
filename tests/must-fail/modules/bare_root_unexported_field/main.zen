// `len` is starred and readable here; `slack` is not, and reaching the
// type through a folder-root alias does not change which is which.

zz = vec

main = (env: Env) Res<i32, IoError> {
    b ::= zz.Bag(len: 0);
    b.add(3);
    println("{}", b.slack);
    Ok(0);
}
