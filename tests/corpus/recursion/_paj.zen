// probeAJ: Alloc-typed LOCAL (not parameter), .create on it
main = (env: Env) Res<i32, AllocError> {
    a: Alloc = env.mem.alloc();
    p = a.create<i64>().try();
    p.write(0, 42);
    println("v {}", p.read(0));
    Ok(0);
}
