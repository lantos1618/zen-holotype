Point: {
    x: i64,
    y: i64,
}
refl<T> = (v: T) i64 {
    s :: i64 = 0
    v.each_field((n, fv) { s = s + fv })
    s
}
wrap1<T> = (v: T) i64 { v.refl() }
wrap2<T> = (v: T) i64 { v.wrap1() }
main = () i32 {
    p = Point(
        x: 1,
        y: 2,
    )
    to_i32(p.wrap2())
}
