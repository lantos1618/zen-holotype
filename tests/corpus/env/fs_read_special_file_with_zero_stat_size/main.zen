FsError = std.env
Error = FsError | AllocError

main = (env: Env) Res<i32, Error> {
    text = env.fs.read(env.mem.alloc(), "/proc/self/status").try();
    println("nonempty {}", !text.is_empty());
    Ok(0)
}
