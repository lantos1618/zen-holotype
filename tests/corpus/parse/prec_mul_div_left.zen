// `*` and `/` are the same precedence and associate LEFT:
// 100 / 5 * 2 is (100 / 5) * 2 = 40, not 100 / (5 * 2) = 10.

main = (env: Env) Res<i32, ArgError> {
    v = 100 / 5 * 2;
    println("{}", v);
    Ok(0);
}
