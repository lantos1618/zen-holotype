// Two directories under the marker, and `app` holds no `app.zen`: the
// holds-its-own-name climb stops here, and only the `build.zen` rule
// climbs the last levels to `src`, where `std` is.
tally* = (xs: Vec<i32>) usize {
    xs.len
}
