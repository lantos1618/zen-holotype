// arms are `pattern => expr`. the `=>` is not optional.
main = (env: Env) Res<i32, ArgError> {
    n = 0;
    label = (n == 0).match({
        true "zero",
        false => "nonzero",
    })
    println(label);
    Ok(0);
}
