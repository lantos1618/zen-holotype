// probeAS: Arena param, raw() via try, in Res fn
mk = (ar: Arena) Res<i64, AllocError> {
    p = ar.raw(8, 16).try();
    q = p.to<i64>();
    q.write(0, 42);
    Ok(q.read(0))
}

main = (env: Env) Res<i32, AllocError> {
    a ::= env.mem.alloc();
    println("v {}", mk(a).try());
    Ok(0);
}
