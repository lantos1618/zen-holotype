// a float literal with no digits after the point. DESIGN.md only ever
// writes 1.0 / 2.0 / 3.14159, always digits on both sides.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    x: f64 = 12.;
    println("{}", x);
    Ok(0);
}
