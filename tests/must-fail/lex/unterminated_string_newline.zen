// a newline arrives before the closing quote. a string literal does not
// span lines, so this is unterminated at the newline -- not a multiline
// string.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("abc
);
    Ok(0);
}
