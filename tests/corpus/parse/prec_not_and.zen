// `!` binds tighter than `&&`: !a && b is (!a) && b = false,
// not !(a && b) = true.

main = (env: Env) Res<i32, ArgError> {
    a = true;
    b = false;
    (!a && b).match({
        true => println("true"),
        false => println("false"),
    })
    Ok(0);
}
