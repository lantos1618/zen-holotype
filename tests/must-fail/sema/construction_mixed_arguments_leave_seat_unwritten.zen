// Positional seat numbers are absolute argument positions, matching codegen.
// position: the construction with x still unwritten.

Point = {
    x: i32,
    y: i32,
}

main = (env: Env) Res<i32, AllocError> {
    p = Point(y: 60, 50);
    println("{} {}", p.x, p.y);
    Ok(0);
}
