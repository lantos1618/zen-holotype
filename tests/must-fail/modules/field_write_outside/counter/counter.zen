// total is readable outside the module; mutation only ever goes
// through exported methods

Counter* = {
    total* :: i32,

    add* = (self :: @Self, n: i32) () {
        self.total = self.total + n;
    }
}
