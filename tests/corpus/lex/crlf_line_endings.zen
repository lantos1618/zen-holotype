// every line in this file ends with CRLF. the lexer must treat \r\n as
// one line break and must not leak the \r into any token or position.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("crlf one");
    println("crlf two");
    Ok(0);
}
