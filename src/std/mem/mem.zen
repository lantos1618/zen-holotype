// Explicit allocation, arenas, and raw pointer operations.

// Allocation interface and failure.
AllocError*, Alloc* = std.mem.mem_alloc

// Arena ownership and page authority.
Arena*, Mem*, Page* = std.mem.mem_arena

// Raw unchecked pointers.
Ptr*, null_ptr* = std.mem.mem_ptr
