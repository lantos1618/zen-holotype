// `Marked` is a name for alpha's `Ring`, and the alias is DECLARED here,
// so its target is the `Ring` THIS file can see.
Ring* = Small | Large

Marked* = Ring

label* = (r: Ring) str {
    r.match({
        Small => "alpha small",
        Large => "alpha large",
    });
}
