> **MOVED.** This M2 capturing-closures design is now the "M2 — Capturing closures" section of
> [`../closures-design.md`](../closures-design.md), the single source of truth for first-class
> function values (M0/M1 non-capturing + M2 capturing). This file is kept only as a redirect.
