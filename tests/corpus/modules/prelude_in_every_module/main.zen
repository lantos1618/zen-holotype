half = work.work

main = (env: Env) Res<i32, IoError> {
    half(8).match({
        Ok(v) => println("{}", v),
        None => println("odd"),
    });
    half(7).match({
        Ok(v) => println("{}", v),
        None => println("odd"),
    });
    Ok(0);
}
