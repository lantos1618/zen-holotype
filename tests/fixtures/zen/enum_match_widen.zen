{ to_exit } = std.core.bool
Msg*: Small | Big
big = () i64 { 4294967298 }
main = () i32 {
    msg := Msg.Big
    got := msg.match ({
        .Small => 1,
        .Big   => big(),
    })
    (got == big()).to_exit()
}
