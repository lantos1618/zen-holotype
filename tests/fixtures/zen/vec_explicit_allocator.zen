halloc = std.mem.heap
vec = std.collections.vec
main = () i32 {
    alloc := halloc.gpa()
    a := alloc.addr()
    v := vec.of(a, [10, 20])
    v2 := v.push(a, 30)
    total := v2.get(0) + v2.get(2)
    v2.free(a)
    total
}
