hm = std.collections.hmap
halloc = std.mem.heap
main = () i32 {
    alloc = halloc.gpa()
    a = alloc.addr()
    k: i64 = 0
    v: i64 = 0
    m ::= hm.of(a, k, v).expect("of")
    m = m.put(a, k, v).expect("put")
    0
}
