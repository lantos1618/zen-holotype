// EOF arrives immediately after an identifier: the file is truncated
// mid-declaration, with no newline at the end.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("hi");
    Ok(0);
}

trailing_identifier