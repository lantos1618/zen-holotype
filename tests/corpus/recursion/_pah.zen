// probeAH: Alloc-typed param, method call on it, but NOT create -- plain
// interface method (none exist with bodies on Alloc... use realloc? no).
// Instead: does a NON-generic bound call through Alloc work?
Noisy2 = {
    tag: str,
}

Loud = {
    n: i64,
}

Loud.impl(Show, {
    show = (self: @Self) str { "loud" }
})

main = (env: Env) Res<i32, IoError> {
    l = Loud(n: 5);
    println("v {}", l.show());
    Ok(0);
}
