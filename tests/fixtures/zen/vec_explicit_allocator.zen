halloc = std.mem.heap
vec = std.collections.vec
{ expect } = std.core.result
main = () i32 {
    alloc = halloc.gpa()
    a = alloc.addr()
    v = vec.of(a.allocator(), [10, 20]).expect("of")
    v2 = v.push(a, 30).expect("push")
    total = v2.get(0) + v2.get(2)
    v2.free(a)
    total
}
