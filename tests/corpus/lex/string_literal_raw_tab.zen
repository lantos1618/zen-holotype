// a raw TAB byte inside a string literal, alongside the \t escape.
// both must produce the same byte.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("raw:	done");
    println("esc:\tdone");
    Ok(0);
}
