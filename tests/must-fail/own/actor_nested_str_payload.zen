// The C mailbox owns direct str arguments, but it emits no slice descriptor
// for a str stored inside another value.

TextBox = { text: str }

Worker = {}

Worker.impl(Actor, {
    process = (self :: @Self, ctx: Context, payload: TextBox) {
        ctx.env.out.println("received")
    }
})

forward = (worker: Ref<Worker>, payload: TextBox) Res<(), ActorError> {
    worker.process(payload).try();
    Ok(())
}

main = (env: Env) Res<i32, AllocError> { Ok(0) }
