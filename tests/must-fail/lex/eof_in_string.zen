// EOF arrives inside a string literal: no closing quote, and the file
// does not end with a newline either.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("unterminated