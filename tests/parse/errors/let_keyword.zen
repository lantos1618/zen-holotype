// MUST FAIL: `let`. The four binding forms are `x = e`, `x ::= e`, `x: T = e`,
// `x: T ::= e` (DESIGN.md:1245-1248, and PLAN.md:135). There is no binding keyword;
// mutability is carried by `::`, not by a word.

main = (env: Env) Res<i32, Error> {
    let x = 1;
    Ok(0);
}
