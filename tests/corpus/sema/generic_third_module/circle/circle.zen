Circle* = {
    radius: i32,
}

// ufcs: a free function whose first parameter is Circle travels with the
// type when it is imported, and calls as a method
area* = (c: Circle) i32 { c.radius * c.radius * 3 }
