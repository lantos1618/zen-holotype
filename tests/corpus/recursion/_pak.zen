// probeAK: Arena-typed param, raw() call -- is generic <T> the trigger?
mk = (ar: Arena) i64 {
    p = ar.raw(8, 16).try();
    q = p.to<i64>();
    q.write(0, 42);
    q.read(0)
}

main = (env: Env) Res<i32, AllocError> {
    a ::= env.mem.alloc();
    println("v {}", mk(a));
    Ok(0);
}
