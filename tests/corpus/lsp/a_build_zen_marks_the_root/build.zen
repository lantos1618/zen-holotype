// THE MARKER, NOT A BUILD. This file's job is to EXIST: root_for reads
// a `build.zen` in an ancestor directory as root evidence — the same
// marker the editors root projects by. The staged tree still compiles
// every module in it, so the marker holds one trivial declaration
// rather than none.
marked = 0
