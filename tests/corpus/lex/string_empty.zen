// the empty string literal, and adjacent quotes that are not one token.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    empty = "";
    println("[{}]", empty);
    println("[{}][{}]", "", "");
    Ok(0);
}
