// a negative literal is not representable in u8: unsigned types have no
// negative side.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    b: u8 = -1;
    println("{}", b);
    Ok(0);
}
