// the type, and its impl of a bound declared in a DIFFERENT module.
// importing Tile pulls its world along, impls included.

Boxy = boxy.boxy

Tile* = {
    side: i32,
}

Tile.impl(Boxy, {
    width: self.side,
    height: self.side,
})
