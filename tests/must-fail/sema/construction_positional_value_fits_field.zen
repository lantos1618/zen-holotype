// A positional value is checked against the field at the same seat.
// position: the value that does not fit.

Point = {
    x: i32,
    y: i32,
}

main = (env: Env) Res<i32, AllocError> {
    p = Point(2.5, 3);
    println("{} {}", p.x, p.y);
    Ok(0);
}
