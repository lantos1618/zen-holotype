// MUST FAIL: unnamed (positional) struct fields. DESIGN.md:87 — "fields mirror
// bindings: `name: T` is set at construction and never reassigned, `name :: T` is
// mutable". Every field in the document is named, and DESIGN.md:329 makes the same
// point one level up for function types: an unnamed slot "says nothing about which
// `i32` is which".

Pair* = {
    f64,
    f64,
}
