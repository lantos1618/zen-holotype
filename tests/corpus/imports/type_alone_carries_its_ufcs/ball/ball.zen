Ball* = {
    tag*: i32,
}

// the decoy: right name, wrong type. importing it must not put it in
// front of the receiver's own member in resolution order.
describe* = (b: Ball) i32 { b.tag * 1000 }
