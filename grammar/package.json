{
  "name": "tree-sitter-zen",
  "version": "0.0.1",
  "private": true,
  "description": "Zen grammar for tree-sitter. Stage 0.1 artifact; outlives the bootstrapper as the editor and LSP grammar.",
  "license": "MIT",
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.13"
  },
  "tree-sitter": [
    {
      "scope": "source.zen",
      "file-types": [
        "zen"
      ],
      "injection-regex": "^zen$"
    }
  ]
}
