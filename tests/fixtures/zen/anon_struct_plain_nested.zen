{ println } = std.text.fmt
mk = () { q: i32 } { { q: 42 } }
nested = () { p: { x: i32, y: i32 }, z: i32 } {
    { p: { x: 1, y: 2 }, z: 3 }
}
main = () i32 {
    println(mk().q)
    b := nested()
    println(b.p.x)
    println(b.p.y)
    println(b.z)
    0
}
