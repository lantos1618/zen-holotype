// A nonzero language result remains distinct from compilation rejection.
main = (env: Env) Res<i32, AllocError> {
    (env.argv.len == 0).match({
        true  => Ok(7),
        false => Ok(7),
    })
}
