// The OTHER module. Nothing here is ever hovered; it exists so that
// `app.zen` beside it has a name it cannot resolve on its own, which is
// the whole subject of the test one directory up.
Point* = {
    x*: i32,
    y*: i32,
}
