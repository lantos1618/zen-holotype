// the same rule for a character literal: a backslash before the line ending
// escapes nothing, because zen has no line continuation. one mistake, one
// diagnostic, anchored at the opening quote.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    c: u8 = '\
    Ok(0);
}
