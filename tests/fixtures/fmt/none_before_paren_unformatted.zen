Opt<T>: Some(T) | None
f = (cond: bool) i32 {
    out: Opt<i32> := .None
    (cond).match ({ true => { out = .Some(5) }, false => {} })
    out.match ({ .Some(v) => v, .None => 0 })
}
main = () i32 { f(true) }
