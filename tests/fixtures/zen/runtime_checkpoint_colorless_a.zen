mem = std.mem.heap
rt = std.concurrent.runtime
{ println } = std.text.fmt
main = () i32 { heap := mem.gpa()  hp := heap.addr()  a := rt.async(hp, 1024)  a.addr().checkpoint()  println(42)  a.addr().checkpoint()  a.addr().free(hp)  0 }
