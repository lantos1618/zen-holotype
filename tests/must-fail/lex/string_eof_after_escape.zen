// The string twin of `eof_after_char_escape`: the file ends `"abc\n` --
// the escape itself SUCCEEDED, so `escape` has said nothing, and the
// unterminated literal is this file's only lexical fault. It must still
// be named, at the opening quote.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("abc\n