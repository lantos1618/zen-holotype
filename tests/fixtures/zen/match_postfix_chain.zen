f = (x: bool) i32 {
    (x.then({ false }, { true })).then({
        return 1
    })
    0
}
main = () i32 {
    x = true
    y = (x.then({ false }, { true })).match ({
        true  => 1,
        false => 2,
    })
    y + f(true)
}
