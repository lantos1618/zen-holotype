Msg: Ask(i64, i64, StringView) | Val(i32) | Stop
handle = (m: Msg) i32 {
    m.match ({
        .Ask(p) => to_i32(p._0 + p._1),
        .Val(v) => v,
        .Stop   => 0,
    })
}
main = () i32 {
    a := Msg.Ask(10, 5, "x")
    handle(a) + handle(Msg.Val(3)) + handle(Msg.Stop)
}
