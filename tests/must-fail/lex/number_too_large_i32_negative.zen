// the literal below is one below i32.MIN. the companion corpus test
// number_i32_min_literal.zen proves -2147483648 is accepted, so this
// pair pins the exact boundary.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    n: i32 = -2147483649;
    println("{}", n);
    Ok(0);
}
