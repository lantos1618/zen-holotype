{ Build } = std.build
build = (b: MutPtr<Build>) void {
    b.install(b
        .exe("linklib-demo")
        .root("src")
        .main("main.zen")
        .out("linklib-bin")
        .sources(["native.c"])
        .link("m"))
}
