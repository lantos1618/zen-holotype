// probeE2: is the interleaving real? println sugar vs env.out.println explicit
Noisy = {
    env: Env,
    tag: str,
}

Noisy.impl(Drop, {
    drop = (self :: @Self) () { self.env.out.println("drop {}", self.tag) }
})

chain = (env: Env, n: i64) i64 {
    (n == 0).match({
        true  => 0,
        false => {
            f = Noisy(env: env, tag: "f");
            env.out.println("frame {}", n);
            chain(env, n - 1) + 1;
        },
    });
}

main = (env: Env) Res<i32, IoError> {
    r = chain(env, 3);
    println("chain {}", r);
    Ok(0);
}
