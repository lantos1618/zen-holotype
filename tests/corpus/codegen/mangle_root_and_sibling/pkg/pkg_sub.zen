// the SIBLING, module `pkg.pkg_sub` — STYLE.md's "siblings repeat the
// folder as a prefix". It declares `run`, and `pkg.pkg_sub` + `run` joins
// to the same string `pkg.pkg` + `sub_run` does.
run* = () i32 { 2 }

Box* = { tag*: i32 }
