// A bodiless env.args<T>() capability is refused at its call site.





















ArgError = std.env

Cfg = {
    name: str,
    count: i64,
}

main = (env: Env) Res<i32, ArgError> {
    c = env.args<Cfg>().try();
    println("parsed");
    Ok(0)
}
