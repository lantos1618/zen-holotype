// A function stored in a local retains its declared arity.
use = (local: (a: i32, b: i32) i32) i32 { local(1) }

main = (env: Env) Res<i32, AllocError> {
    println("{}", use((a: i32, b: i32) i32 { a + b }));
    Ok(0)
}
