// no imports at all: std.core is auto-imported into every module, so
// Res, Ok, None and .match are in scope here exactly as in main

half* = (n: i32) Res<i32> {
    (n % 2 == 0).match({
        true => Ok(n / 2),
        false => None,
    })
}
