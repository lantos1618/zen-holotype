// the classic: \" escapes the quote, so it does NOT close the string.
// the literal runs on to the end of the line and is unterminated.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("abc\");
    Ok(0);
}
