// The method's T accepts the concrete free-function signature, so both
// declarations would accept `w.pick(i32)` and resolution would be ambiguous.

W = {
    pick = <T>(self: @Self, value: T) i32 { 1 }
}

pick = (w: W, value: i32) i32 { 2 }

main = (env: Env) Res<i32, AllocError> { Ok(0) }
