// one of two modules declaring `Diag`. this one's `render` is a FREE
// function taking a Diag first, which UFCS makes `d.render(..)` too
Diag* = { code*: usize }

render* = (self: Diag, tag: usize) usize { self.code + tag }
