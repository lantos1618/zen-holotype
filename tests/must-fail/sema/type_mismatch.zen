// the floor: a declared type is checked against what is bound to it.
//
// position: the offending expression.

main = (env: Env) Res<i32, AllocError> {
    n: i32 = "hello";
    println("{}", n);
    Ok(0);
}
