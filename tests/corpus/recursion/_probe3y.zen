Link = {
    value: i64,
    next: Ptr<Link>,
}

main = (env: Env) Res<i32, AllocError> {
    alloc ::= env.mem.alloc();
    helper ::= alloc.create<Link>().try();
    helper.write(0, Link(value: 1, next: null_ptr<Link>()));
    println("{}", helper.read(0).value);
    Ok(0);
}
