// probeL: callee prints via println SUGAR as println arg
three = (env: Env) i64 {
    println("one");
    3
}

main = (env: Env) Res<i32, IoError> {
    println("chain {}", three(env));
    Ok(0);
}
