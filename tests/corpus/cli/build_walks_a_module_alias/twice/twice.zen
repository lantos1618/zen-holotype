// Reached only through the alias beside this file. The import is what
// makes being reached visible: there is no `std` under `src/twice`, so
// opening this file is what produces the one line the test expects.
Alloc = std.mem

value* = (n: i32) i32 { n + n }
