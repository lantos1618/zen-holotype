// the cycle through two types. same defect, and the one a naive
// "does this struct mention itself" check misses.
//
// position: the field that closes the cycle.

Room = {
    door: Door,
}

Door = {
    room: Room,
}

main = (env: Env) Res<i32, AllocError> {
    Ok(0);
}
