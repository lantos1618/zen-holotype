# bootstrap/zenc.gen.c is the generated compiler seed (C emitted from .zen sources).
# It conflicts on almost every parallel branch. On merge/rebase conflict, pick either
# side (`git checkout --theirs bootstrap/zenc.gen.c` is fine) then `make resolve-seed`
# to regenerate the byte-exact seed from source.
