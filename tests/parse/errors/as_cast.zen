// MUST FAIL: an `as` cast operator. `as` occurs in DESIGN.md exactly once, as an
// ordinary method — `circle.as(Display)` (DESIGN.md:145, 141), which builds a fat
// trait value. No cast operator of any spelling is defined anywhere in the document,
// so `n as f64` is two juxtaposed expressions.

main = (env: Env) Res<i32, Error> {
    n = 1;
    f = n as f64;
    Ok(0);
}
