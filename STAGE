4  # PLAN.md stage this tree is graded against. Stage 5 (actors, runtime,
   # @meta) has not arrived, so tests carrying `.stage 5` are deferred.
   #
   # 4 IS THE GRADE, NOT A CLAIM THAT 1-4 ARE FINISHED. Two things below
   # it are still missing and are meant to show up as failures rather
   # than be graded away: stage 2 has no formatter, so `make fmt` cannot
   # pass, and stage 4 has no LSP. Lowering this number would defer
   # their tests and make the board read better without a line of work
   # being done, which is the one thing it must not do.
