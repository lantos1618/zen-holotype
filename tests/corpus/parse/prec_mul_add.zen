// `*` binds tighter than `+`: 2 + 3 * 4 is 2 + (3 * 4), not (2 + 3) * 4.
// ASSUMPTION: DESIGN.md states no precedence table at all. see the report.

main = (env: Env) Res<i32, ArgError> {
    v = 2 + 3 * 4;
    println("{}", v);
    Ok(0);
}
