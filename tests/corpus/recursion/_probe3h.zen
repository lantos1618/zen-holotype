Link = {
    value: i64,
    next: Ptr<Link>,
}

build = (alloc: Alloc, n: i64) Link {
    (n <= 0).match({
        true  => 1000,
        false => 7,
    });
}

main = (env: Env) Res<i32, AllocError> {
    Ok(0);
}
