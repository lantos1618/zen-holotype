Item = {
    id: i32,
    read = (self: @Self) i32 { self.id }
}
Item.impl(Drop, {
    drop = (self :: @Self) () { println("drop {}", self.id); }
})
main = () i32 {
    item = Item(id: 7);
    println("read {}", Item.read(item));
    Item.drop(consume item);
    0
}
