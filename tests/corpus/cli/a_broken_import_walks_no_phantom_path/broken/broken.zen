// OVERWRITTEN AT RUNTIME by the test one directory up. The broken
// import this build is about cannot ship as a `.zen` file: the `fmt`
// gate formats every `.zen` under tests/corpus, and a file whose whole
// point is that it does not parse cannot be formatted. This comment
// keeps the directory — and `zen fmt` — happy.
