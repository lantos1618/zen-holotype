{ Build } = std.build
build = (b: MutPtr<Build>) void {
    b.install(b
        .exe("build-native")
        .root("src")
        .main("main.zen")
        .out("native-bin")
        .sources(["native.c"])
        .cimport("util"))
}
