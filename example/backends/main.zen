fib = (n: i32) i32 {
    (n < 2).match({
        true  => n,
        false => fib(n - 1) + fib(n - 2),
    })
}

main = () i32 {
    number: i32 = 10;
    println("fib({}) = {}", number, fib(number));
    0
}
