// `Latch` is internal to module `vec`. Qualifying through a folder-root
// alias is not an export: an unstarred TYPE has to be as invisible as an
// unstarred function is.

zz = vec

main = (env: Env) Res<i32, IoError> {
    e = zz.Latch(hash: 1);
    println("{}", e.hash);
    Ok(0);
}
