{ println } = std.io.print
mk = () Answer { Answer(q: 42) }
nested = () Nested {
    Nested(
        p: Coordinates(
            x: 1,
            y: 2,
        ),
        z: 3,
    )
}
main = () i32 {
    println(mk().q)
    b = nested()
    println(b.p.x)
    println(b.p.y)
    println(b.z)
    0
}
Answer: {
    q: i32,
}
Coordinates: {
    x: i32,
    y: i32,
}
Nested: {
    p: Coordinates,
    z: i32,
}
