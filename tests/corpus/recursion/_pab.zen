// probeAB: Alloc parameter, .try() with parens
go = (a: Alloc) i64 {
    p = a.create<i64>().try();
    p.write(0, 9);
    p.read(0)
}

main = (env: Env) Res<i32, AllocError> {
    a ::= env.mem.alloc();
    println("v {}", go(a));
    Ok(0);
}
