// THE OTHER MODULE, AND IT IS CORRECT ON DISK — this test's wrong
// program arrives through the overlay, because `tests/run.py` compiles
// every module it stages, so a broken file on disk fails the test
// before the test runs.
Point* = {
    x*: i32,
    y*: i32,
}
