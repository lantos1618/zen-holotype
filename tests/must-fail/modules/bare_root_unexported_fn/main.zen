// The `*` gate, reached through the FOLDER ROOT rather than `vec.vec`.
// Every other test in this directory writes the dotted form, so all of
// them exercised one path into the gate and none of them exercised this.

zz = vec

main = (env: Env) Res<i32, IoError> {
    println("{}", zz.reseat(3));
    Ok(0);
}
