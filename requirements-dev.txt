# Dev deps for the Python-free test suite: only pytest (the test RUNNER — it drives the `zenc`
# binary via tests/_oracle.py and imports zero zen.* code). mypy is gone with the Python frontend
# it used to type-check (Stage D). pytest itself is not a build dependency.
pytest>=8.0
