// the first declarer of `Box`. same name, same field list, different
// arithmetic from crate's: 100 here, 1000 there.
Box* = {
    label*: i32,
}

make* = (n: i32) Box { Box(label: n) }

describe* = (b: Box) i32 { b.label * 100 }
