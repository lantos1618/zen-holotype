// src/lsp/lsp_decl.zen
// A re-export. The finder itself is `src/std/ast/ast_named.zen`, which
// owns it since `definition` became the second caller (`STYLE.md`'s
// second-caller rule); this line keeps `lsp_hover.zen`'s and the folder
// root's imports as they were.

Tell*, TypeAt*, ValueAt*, told_at*, is_nothing* = std.ast.ast_named
