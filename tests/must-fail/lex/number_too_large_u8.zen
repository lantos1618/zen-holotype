// the literal below does not fit u8; the boundary value does -- see
// corpus/lex/number_type_boundaries.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    b: u8 = 256;
    println("{}", b);
    Ok(0);
}
