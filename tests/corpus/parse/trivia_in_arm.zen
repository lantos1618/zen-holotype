// a comment INSIDE a match arm's body.

main = (env: Env) Res<i32, ArgError> {
    n = 0;
    (n == 0).match({
        true  => {
            // inside the arm body
            println("{}", n + 6 * 7 + 1);
        },
        false => println("{}", n),
    });
    Ok(0);
}
