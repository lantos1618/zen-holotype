// Agreement is not legality: two strings have one type, but the language
// supplies no arithmetic operator for that type.

add = (left: str, right: str) str { left + right }

main = (env: Env) Res<i32, AllocError> { Ok(0) }
