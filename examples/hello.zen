// hello.zen — the smallest real program: imports, output, an exit code.
//   $ zenc run examples/hello.zen
{ println, println_int } = std.fmt

main = () i32 {
    println("hello, zen")
    println_int(6 * 7)
    0
}
