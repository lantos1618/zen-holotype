# The compiler is the Python-free `zenc` binary (cc + bootstrap/Makefile); the build has NO
# Python runtime dependency. The former tree_sitter/setuptools deps powered the deleted Python
# reference frontend (Stage D) and are gone.
