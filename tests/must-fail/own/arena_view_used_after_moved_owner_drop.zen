main = (env: Env) Res<i32, AllocError> {
    arena ::= env.mem.alloc();
    text = "hello".dup(arena).try();
    moved = consume arena;
    (consume moved).drop();
    env.out.println("{}", text);
    Ok(0)
}
