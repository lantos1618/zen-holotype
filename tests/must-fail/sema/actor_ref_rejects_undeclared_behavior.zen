Actor, Ref, Context = std.actor

Worker = {}

Worker.impl(Actor, {
    receive = (self :: @Self, ctx: Context, message: i32) ()
})

bad = (receiver: Ref<Worker>) () {
    receiver.missing(1);
}

main = (env: Env) i32 { 0 }
