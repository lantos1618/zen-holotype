{ ActorRef } = std.concurrent.actor
Msg*: Poke(MutPtr<i32>)
fwd<T> = (h: ActorRef<Msg>, payload: T) void { h.send(.Poke(payload)) }
run = (h: ActorRef<Msg>, p: MutPtr<i32>) void { fwd(h, p) }
main = () i32 { 0 }
