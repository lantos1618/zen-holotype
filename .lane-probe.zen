main = (env: Env) Res<i32, AllocError> {
    n = count('7' == '7');
    println("{}", n);
    Ok(0);
}

count = (b: bool) i32 {
    good ::= 0;
    b.match({
        true  => { good = good + 1; }
        false => println("lost an ascii byte"),
    });
    good
}
