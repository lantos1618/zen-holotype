// A fixed-array count is never negative, even when the initializer could
// otherwise give codegen a concrete storage shape.
main = (env: Env) Res<i32, AllocError> {
    values: [u8, -1] = [1, 2];
    println("{}", values[0]);
    Ok(0)
}
