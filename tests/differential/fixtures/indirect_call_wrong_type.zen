// Parenthesizing an indirect callee must not erase its parameter types.
use = (pick: (value: i32) i32) i32 { (pick)("wrong") }

main = (env: Env) Res<i32, AllocError> {
    println("{}", use((value: i32) i32 { value }));
    Ok(0)
}
