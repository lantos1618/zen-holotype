// a digit followed by letters is neither an identifier nor a number:
// an identifier may not start with a digit, and a number may not carry
// a letter suffix.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    1abc: i32 = 1;
    println("hi");
    Ok(0);
}
