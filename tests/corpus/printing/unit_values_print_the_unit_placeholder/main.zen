// Unit has no C storage, but its printable spelling is still `()`.
// Pin both a format hole and the value-only console form.

Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("unit {}", ());
    println(());
    Ok(0);
}
