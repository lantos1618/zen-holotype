Chooser = {
    choose* ::= <T>(self: @Self, value: T) T
}

Tool = {}

Tool.impl(Chooser, {
    choose = <T>(self: @Self, value: T) T { value }
})

main = (env: Env) Res<i32, AllocError> {
    text: str = "wrong";
    Tool().choose<i32>(text);
    Ok(0);
}
