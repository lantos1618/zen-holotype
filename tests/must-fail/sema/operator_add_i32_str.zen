// Both sides of arithmetic must be numbers of one settled type; C must not
// become the checker for a string passed to the i32 addition helper.

add = (n: i32, text: str) i32 { n + text }

main = (env: Env) Res<i32, AllocError> { Ok(0) }
