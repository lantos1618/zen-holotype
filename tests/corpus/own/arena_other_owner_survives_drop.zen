main = (env: Env) Res<i32, AllocError> {
    first  = env.mem.alloc();
    second = env.mem.alloc();
    text   = "hello".dup(second).try();
    (consume first).drop();
    env.out.println("{}", text);
    Ok(0)
}
