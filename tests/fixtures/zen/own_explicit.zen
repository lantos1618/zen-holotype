halloc = std.mem.heap
{ Resource, new_in } = std.mem.own
main = () i32 {
    alloc := halloc.gpa()
    o := alloc.addr().new_in(Resource(
        id: 7,
        slot: 0,
    ))
    n := o.get().id
    o.release_in(alloc.addr())
    n
}
