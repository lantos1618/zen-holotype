// DESIGN's named case: Vec travels with add, never with grow

Vec* = {
    len* :: i32,

    add* = (self :: @Self, n: i32) () {
        self.grow();
        self.len = self.len + n;
    }

    grow = (self :: @Self) () {
        self.len = self.len + 1;
    }
}
