// zero, and zero in every position it can appear.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    z: i32 = 0;
    println("{}", z);
    println("{}", z + 0);
    println("{}", 0 * 12345);
    u: u8 = 0;
    println("{}", u);
    big: u64 = 0;
    println("{}", big);
    Ok(0);
}
