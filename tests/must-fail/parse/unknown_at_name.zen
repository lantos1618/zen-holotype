// STYLE.md: the `@` namespace has exactly three entries -- @Self,
// @meta, @scope -- and adding a fourth is a design change. `@foo` is
// not a name a user may introduce.
main = (env: Env) Res<i32, ArgError> {
    n = @foo(1);
    println("{}", n);
    Ok(0);
}
