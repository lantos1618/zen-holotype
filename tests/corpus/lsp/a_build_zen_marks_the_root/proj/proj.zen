// THE WELL-FORMED SHELL around the directory that matters. lint's
// dir-module-shape rule reads a test's IMMEDIATE subdirectories, so the
// folder that holds no own-name file — `proj/app`, the shape under
// test — lives one level down, inside a module that is shaped right.
shell = 0
