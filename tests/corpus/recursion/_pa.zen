// probeA: deep linear recursion, 100k frames, i64 total past i32 range
deep_sum = (n: i64) i64 {
    (n <= 0).match({
        true  => 0,
        false => n + deep_sum(n - 1),
    });
}

main = (env: Env) Res<i32, AllocError> {
    println("{}", deep_sum(100000));
    Ok(0);
}
