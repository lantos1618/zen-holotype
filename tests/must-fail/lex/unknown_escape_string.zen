// a backslash followed by q is not an escape sequence. an unknown
// escape is an error, never a silent q.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    println("bad \q escape");
    Ok(0);
}
