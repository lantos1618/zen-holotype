4  # PLAN.md stage this tree is graded against. Stage 5 (actors, runtime,
   # @meta) has not arrived, so tests carrying `.stage 5` are deferred.
   #
   # 4 IS THE GRADE, NOT A CLAIM THAT 1-4 ARE FINISHED. What is still
   # missing below it is meant to show up as a failure rather than be
   # graded away: stage 4 has no LSP, and stage 2's formatter models the
   # FILE and not yet the DECLARATION -- so `make fmt` runs, and is red
   # on the eighteen files whose blank lines it disagrees with.
   # Lowering this number would defer their tests and make the board
   # read better without a line of work being done, which is the one
   # thing it must not do.
