# scripts/difftest-allow.txt — intentional, individually justified behavior diffs (case-id:stage).
# Consumed by scripts/difftest.sh; empty of entries means the gate demands byte-identity.
# B4/R3 sig-driven AliasCands: a USER-written `g__extra` decl (main body, merely SHAPED like an
# alias decl) is no longer a UFCS candidate for `p.extra()` — the alias's method namespace is closed
# to the aliased module's REAL exports. Pre-R3 the shape scan dispatched it (accepted, ran, exit
# 106); post-R3 it is method-not-found. Pinned by the fixture; all three stages diverge.
tests/fixtures/dispatch/alias_shaped_user_decl:check
tests/fixtures/dispatch/alias_shaped_user_decl:emit
tests/fixtures/dispatch/alias_shaped_user_decl:run
