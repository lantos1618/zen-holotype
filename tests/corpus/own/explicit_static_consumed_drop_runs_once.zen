Item = { id: i32 }
Item.impl(Drop, { drop = (self :: @Self) () { println("drop {}", self.id); } })

main = (env: Env) Res<i32, AllocError> {
    arena = env.mem.alloc();
    Arena.drop(consume arena);
    item = Item(id: 7);
    Item.drop(consume item);
    println("done");
    Ok(0)
}
