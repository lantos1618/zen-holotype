// C promotes bools for multiplication, but Zen's arithmetic domain excludes
// bool so the promotion cannot silently define language behavior.

multiply = (left: bool, right: bool) bool { left * right }

main = (env: Env) Res<i32, AllocError> { Ok(0) }
