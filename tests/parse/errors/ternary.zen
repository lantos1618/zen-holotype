// MUST FAIL: the ternary conditional. DESIGN.md:152 — "No `if`, no ternary, no `?`
// operator." The one-sided form is `bool.then` (DESIGN.md:423); the two-sided form
// is `.match` (DESIGN.md:1257).

main = (env: Env) Res<i32, Error> {
    label = (1 == 0) ? "zero" : "nonzero";
    Ok(0);
}
