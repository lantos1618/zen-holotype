// MUST FAIL: `fn`. DESIGN.md:1166 — "functions are just bindings of lambdas, so
// they're values". The form is `name = (params) Ret { .. }` (DESIGN.md:1016, 1168).
// There is no function-declaration keyword anywhere in the document.

fn add(a: i32, b: i32) i32 {
    a + b
}
