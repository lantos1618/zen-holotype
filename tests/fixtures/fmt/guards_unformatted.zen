Box: Val(i32) | Empty
sz = (o: Box) str {
o.match({
.Val(v) if v > 10 => "big",
.Val(v) => "small",
.Empty => "none"
})
}
tier = (o: Box) i32 {
o.match({ .Val(v) if v > 100 => 3, .Val(v) if v > 10 => 2, .Val(v) => 1, .Empty => 0 })
}
main = () i32 { 0 }
