main = (env: Env) Res<i32, AllocError> {
    arena = env.mem.alloc();
    text ::= "old".dup(arena).try();
    (consume arena).drop();
    text = "hello";
    env.out.println("{}", text);
    Ok(0)
}
