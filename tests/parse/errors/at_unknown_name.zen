// MUST FAIL: `@foo`. DESIGN.md:302 — "`@` is the compiler's namespace ... it is one
// flat namespace, deliberately small, and everything in it is documented here."
// The complete set is @Self, @meta, @scope. `@` is "not a sigil with meaning of its
// own", so `@` + an arbitrary identifier is not a form.

main = (env: Env) Res<i32, Error> {
    x = @foo(1);
    Ok(0);
}
