backend_name, emit_c, emit_h, header_name = gen.gen

main = (env: Env) Res<i32, IoError> {
    println(backend_name());
    println("{}", emit_c(1));
    println("{}", emit_h(1));
    println(header_name());
    Ok(0);
}
