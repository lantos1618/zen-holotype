// one import line, one name, two declarations behind it
Circle, Square, label = shape.shape

main = (env: Env) Res<i32, IoError> {
    println(Circle(radius: 1).label());
    println(Square(side: 2).label());
    Ok(0);
}
