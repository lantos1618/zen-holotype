// Mode has no star, so neither Mode nor Fast nor Slow leaves here

Mode = Fast | Slow

speed* = (quick: bool) i32 {
    m = quick.match({
        true => Fast,
        false => Slow,
    });
    m.match({
        Fast => 10,
        Slow => 1,
    });
}
