// a comment before a declaration. it attaches to `Point`, not to the
// file, and `zen fmt` must leave it on these two lines.
Point = {
    // and one before a field
    x: i32,
    y: i32,
}

main = (env: Env) Res<i32, ArgError> {
    p = Point(x: 1, y: 2);
    println("{}", p.y);
    Ok(0);
}
