// the literal below is one past u64.MAX, so it fits no integer type at
// all: the lexer cannot even hold it in a u64 accumulator.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    n: u64 = 18446744073709551616;
    println("{}", n);
    Ok(0);
}
