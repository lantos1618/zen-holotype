Opt<T>: Some(T) | None
f = (cond: bool) i32 {
    out: Opt<i32> := .None
    cond.then({
        out = .Some(5)
    })
    out.match ({
        .Some(v) => v,
        .None    => 0,
    })
}
main = () i32 { f(true) }
