// the literal below is one past i32.MAX, and there is no unary minus in
// front of it to make it i32.MIN.
Error = AllocError | IoError

main = (env: Env) Res<i32, Error> {
    n: i32 = 2147483648;
    println("{}", n);
    Ok(0);
}
