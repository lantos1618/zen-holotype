// Looking through a constant name must retain the sign of the value it
// names; a negative count cannot silently become a zero-length array.

NEGATIVE: i64 = -1

main = (env: Env) Res<i32, AllocError> {
    values: [u8, NEGATIVE] = [1, 2];
    println("{}", values[0]);
    Ok(0);
}
