Clock = {
    v :: i32,
    since_start* = (self: @Self) i32 { self.v }
}

Res = {
    v :: i32,
    get* = (self: @Self) i32 { self.v }
}

run* = () () {
    clock: Clock = Clock(v: 22);
    result: Res  = Res(v: 5);
    println("{} {}", clock.since_start(), result.get());
}
