// THE OTHER MODULE, CORRECT ON DISK. `Point` is the type the open
// buffer's names resolve THROUGH, so the classified answer crosses a
// module boundary or it is not the build's answer at all.
Point* = {
    x*: i32,
    y*: i32,
}
